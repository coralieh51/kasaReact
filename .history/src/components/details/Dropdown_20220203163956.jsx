import React from "react";

function Dropdown({ currentHousing, id }) {
    const currentDescription = [];
    currentDescription.push(currentHousing.description);
    return (
        console.log({currentHousing.description})
    )

}

export default Dropdown;
