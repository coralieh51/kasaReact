import logements from "../../assets/data/logements.json"

function Card() {
    return(
        logements.map(logementL. => {
            <div>logement[0].name</div>
        })
    )
}

export default Card;